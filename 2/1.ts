// Personal Message: Store a person’s name in a variable, and print a message to that person. Your message should be simple, such as, “Hello Eric, would you like to learn some Python today?” write a code  in typescript
//  name
const personName: string = 'arfa';

// Create the message
const message: string = `Hello ${personName}, would you like to learn some Python today?`;

// Print the message
console.log(message);
